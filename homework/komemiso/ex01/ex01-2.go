package main

import "fmt"

func main() {
	a := 3
	b := 5
	fmt.Println("a + b = ", (a + b))
	fmt.Println("a - b = ", (a - b))
	fmt.Println("a * b = ", (a * b))
	fmt.Println("a / b = ", (a / b))
	fmt.Println("a % b = ", (a % b))
	fmt.Println("a * a = ", (a * a))
}
